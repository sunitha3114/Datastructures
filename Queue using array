#include <stdio.h>

#define MAX 5

int queue[MAX];
int front = -1, rear = -1;

void enqueue() {
    int x;
    if (rear == MAX - 1)
        printf("Queue Overflow\n");
    else {
        printf("Enter element: ");
        scanf("%d", &x);

        if (front == -1)
            front = 0;

        queue[++rear] = x;
    }
}

void dequeue() {
    if (front == -1 || front > rear)
